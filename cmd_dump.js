const vscode = require('vscode');

// This needs to run inside VS Code context, so I'll just temporarily mod the extension to do this logging, or I can use the same discover approach but with different keywords.
